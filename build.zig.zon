.{
    .name = "2024",
    .version = "0.0.1",

    // Each dependency must either provide a `url` and `hash`, or a `path`.
    .dependencies = .{
        // See `zig fetch --save <url>`.
    },

    // List files required for compilation plus any license(s).
    // The zig package manager will only use these files (and dirs).
    .paths = .{
        "build.zig",
        "build.zig.zon",
        "src",
        //"LICENSE"
    },
}